import { CalculeTimePipe } from './calcule-time.pipe';

describe('CalculeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
